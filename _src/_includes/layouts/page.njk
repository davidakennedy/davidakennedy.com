{% extends 'layouts/base.njk' %}
{% block content %}
  <section class="page">
    <div class="page-header">
      {%- if featuredimg and image -%}
        {% img "", "", image, imgalt, "" %}
      {%- endif -%}
      <h1 class="title p-name">{{ title or renderData.title }}</h1>
    </div>

    <div class="page-content">
      {{ content | safe }}
    </div>
  </section>
{% endblock %}

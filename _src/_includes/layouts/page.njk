{% extends 'layouts/base.njk' %}
{% block content %}
  <section class="page">
    <header class="page-header">
      {%- if image and featuredimg -%}
      {% img "wide-width", "", image, imagealt, "" %}
      {%- endif -%}
      <h1 class="title p-name">{{ title or renderData.title }}</h1>
    </header>

    <div class="page-content">
      {{ content | safe }}
    </div>
  </section>
{% endblock %}

---
title: Tags
description: A list of topics for David A. Kennedy's blog.
eleventyNavigation:
  key: Tags
  parent: Just Call Me DK
layout: page
---
{% block content %}
  {% if collections.tagList %}
    <ul>
      {% for tag in collections.tagList | sort( false, false) %}
      {% set tagUrl %}/tag/{{ tag | slugify }}/{% endset %}
        <li>
          <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
          {% if (collections[ tag ].length) > 1  %}
          ({{ collections[tag].length }} posts)
          {% else %}
          ({{ collections[tag].length }} post)
          {% endif %}
        </li>
      {% endfor %}
    </ul>
  {% endif %}
  {% endblock %}
